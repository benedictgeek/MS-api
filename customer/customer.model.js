const mongoose = require("mongoose");

// Customer Model

mongoose.model("Customer", {
  name: {
    type: String,
    required: true,
  },
  email: {
    type: String,
    required: true,
  },
  phone: {
    type: Number,
    required: true,
  },
});
